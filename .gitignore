# Internal, not for public release
PROVENANCE.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.ipynb_checkpoints/

# Runs & outputs (repo-root only; leading slash so we don't ignore package dirs
# like intertreeseg/data/ that happen to share these names)
/log/
/result/
*.pth
*.ckpt
# (weights are distributed via Google Drive, not committed; see checkpoints/README.md)

# Data (do not commit datasets / generated blocks) — repo-root folder only
/data/
*.h5
*.ply

# Node / frontend build artifacts
node_modules/
app/frontend/dist/
*.tsbuildinfo

# Env / editor
.venv/
venv/
.idea/
.vscode/
.DS_Store
