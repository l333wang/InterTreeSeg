# Runtime dependencies for InterTreeSeg.
# Versions pinned to the tested `pointcept` conda env inside the `ptv3:latest`
# Docker image (Python 3.8, CUDA 11.8, RTX 3090 Ti).
#
# NOTE: torch / spconv / flash_attn / torch_scatter are CUDA-build specific and
# are usually installed from the provider's index, not plain PyPI. The versions
# below document what the reference environment ships.

torch==2.1.0            # CUDA 11.8 build
spconv-cu118==2.3.6     # sparse conv backend for PTv3
flash-attn==2.6.3       # optional but enabled by default (enable_flash)
torch-scatter==2.1.2

timm>=1.0.9             # DropPath
addict>=2.4.0           # Dict used by the vendored PTv3 model
numpy
pandas
h5py>=3.11
plyfile
PyYAML>=6.0
tqdm
scikit-learn>=1.3       # instance-level evaluation metrics
open3d>=0.18            # ply I/O in evaluation
hilbertcurve            # ONLY needed by tools/prepare_data.py (Hilbert serialization); not needed for train/infer
